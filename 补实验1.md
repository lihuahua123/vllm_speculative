可以直接复用你现在这套 `stress_tests`，而且复用度会很高。

**现状判断**

你已经有完整输入了：

- 入口脚本：[run_nightjar_stress_tests.sh](/root/autodl-tmp/nightjar/vllm_speculative/benchmarks/run_nightjar_stress_tests.sh:1)
- 汇总索引：[stress_tests_summary.csv](/root/autodl-tmp/nightjar/vllm_speculative/benchmark_results/stress_tests/stress_tests_summary.csv:1)
- 三个 case 的 trace plan：
  [burst_spike.json](/root/autodl-tmp/nightjar/vllm_speculative/benchmark_results/stress_tests/traces/burst_spike.json:1)
  [high_low_oscillation.json](/root/autodl-tmp/nightjar/vllm_speculative/benchmark_results/stress_tests/traces/high_low_oscillation.json:1)
  [sync_migration_worst_case.json](/root/autodl-tmp/nightjar/vllm_speculative/benchmark_results/stress_tests/traces/sync_migration_worst_case.json:1)
- 事件日志：每个 case 下的 `nightjar_events.jsonl`

而且这三组结果已经真的触发了你要的行为：

- 每个 case 都有 `1` 次 `gamma=0`
- 每个 case 都有 `1` 次 `memory_expand`
- 每个 case 都有 `1` 次 `memory_contract`
- 每个 case 都有 `1` 次 `kv_block_migration`

所以最优方案是：

1. 不改 `run_benchmark_tests.py`
2. 不改 stress trace 生成逻辑
3. 只补一个后处理脚本
4. 在 `run_nightjar_stress_tests.sh` 末尾自动调用它

**建议补什么**

建议新增一个专门回答 reviewer 的分析脚本，比如：

- [vllm_speculative/exps/analyze_stress_disable_cases.py](/root/autodl-tmp/nightjar/vllm_speculative/exps/analyze_stress_disable_cases.py)

输入：

- `stress_tests_summary.csv`

读取：

- 每行的 `event_log`
- 对应 pattern 的 trace json

输出两类产物：

1. 一个摘要表 `stress_disable_summary.csv`
2. 一张 case-study 图 `stress_disable_timeline.pdf`

**摘要表建议字段**

最少这些就够 rebuttal 用：

- `pattern`
- `disable_step_count`
- `first_disable_ts`
- `disable_batch_size`
- `disable_queue_len`
- `disable_running_len`
- `disable_free_gpu_blocks`
- `offload_count`
- `reload_count`
- `migration_count`
- `expand_trigger_ts`
- `restore_trigger_ts`
- `time_expand_to_disable_ms`
- `time_contract_to_disable_ms`
- `persist_steps`
- `increase_block_threshold`
- `decrease_block_threshold`

这张表能直接回答：

- 什么时候关 speculation
- 关之前系统处于什么负载
- 是否真的伴随内存扩展/恢复
- 触发逻辑是否来自持续低空闲块而不是偶然抖动

**图怎么画**

一张图 3 行子图，横轴统一时间，按 case 分列或逐 case 单图都行。

每个 case 画：

- 子图 1：`gamma` trace
  - `gamma=0` 区间高亮
  - 画竖线标 `memory_policy_decision`
  - 再标 `memory_expand / memory_contract`

- 子图 2：`free_gpu_blocks` 和阈值线 `2000`
  - 让 reviewer 一眼看到是“持续低于阈值后触发”
  - 再对应 `persist_steps=3`

- 子图 3：`batch_size` 或 `num_running + queue_len`
  - 说明关闭 speculation 对应的是高压段，不是低负载段随机发生

如果想更省事，只做一张 combined plot：

- 上：`gamma`
- 中：`free_gpu_blocks`
- 下：`batch_size`

已经足够。

**为什么说这套方案最省改动**

因为你现有日志已经有这些核心字段：

- `proposal_length_gamma`
- `speculation_enabled`
- `free_gpu_blocks`
- `queue_len`
- `num_running`
- `memory_policy_decision`
- `memory_expand`
- `memory_contract`
- `kv_block_migration`

而且现有 [plot_dynamic_behavior_report.py](/root/autodl-tmp/nightjar/vllm_speculative/exps/plot_dynamic_behavior_report.py:1) 已经有一套成熟的 event-log 解析逻辑。最稳妥的做法不是重写，而是直接复用它的：

- `load_run_data`
- 事件分类方式
- 汇总写 CSV 的套路

**具体改法**

我建议两步。

1. 新增一个轻量脚本 `analyze_stress_disable_cases.py`
   只做 stress-test 专项分析，避免把 `plot_dynamic_behavior_report.py` 搞得太复杂。

2. 在 [run_nightjar_stress_tests.sh](/root/autodl-tmp/nightjar/vllm_speculative/benchmarks/run_nightjar_stress_tests.sh:1) 最后加一段：
```bash
python "$ROOT_DIR/exps/analyze_stress_disable_cases.py" \
  --summary-csv "$SUMMARY_CSV" \
  --trace-dir "$TRACE_DIR" \
  --output-dir "$RESULT_DIR/figs" \
  --prefix "stress_disable"
```

这样你以后只要重跑原 stress tests，分析图表会自动更新。

**你这三组现有结果已经能写出的核心结论**

从当前日志可以直接提炼：

- `burst_spike`
  - 在 `free_gpu_blocks=1774 < 2000` 且连续 `persist_steps=3` 后触发 `expand_kv_cache`
  - 随后出现一次 `gamma=0` step，发生在 `batch_size=42`
  - 同时有 `memory_expand / kv_block_migration / memory_contract`

- `high_low_oscillation`
  - 在 `free_gpu_blocks=1958` 时触发扩容决策
  - 同样出现一次 `gamma=0` step，`batch_size=42`

- `sync_migration_worst_case`
  - 在 `free_gpu_blocks=1800`、`running_len=86` 时触发
  - 出现一次 `gamma=0` step，`batch_size=40`

这足够支撑一句很强的 rebuttal：

> Under bursty and oscillating high-pressure workloads, Nightjar disables speculation only after sustained memory pressure persists for the configured persistence window, rather than reacting to transient fluctuations. Across all three stress patterns, the `γ=0` period coincides with a preceding KV-cache expansion decision and CPU-GPU memory reconfiguration event.

**推荐最后放到论文/response 的形式**

正文或附录：

- 1 张 timeline 图
- 1 张 summary table

response letter 里：

- 先说你新增了 `stress-case disablement analysis`
- 再说用了三种动态负载模式
- 明确报告了：
  - `γ=0` 的时段
  - offload/reload 次数
  - 触发时刻
  - `free_gpu_blocks` 与阈值关系
- 最后给出结论：
  - Nightjar 是在持续内存压力下关闭 speculation，不是随机关闭
  - 关闭与 KV 扩容和迁移事件一致
  - 负载恢复后会重新启用 speculation

如果你要，我下一步可以直接给你把这个后处理脚本和 `run_nightjar_stress_tests.sh` 的接线补出来。

  cd /root/autodl-tmp/nightjar/vllm_speculative
  bash benchmarks/run_nightjar_stress_tests.sh



  要正面回答这个 reviewer，单靠现在的 Mean reload latency 不够，因为它不是“请求真正会感受
  到的延迟”。更合适的是把指标分成两类：传输本身的代价，和它对在线请求的扰动。

  建议统计的字段

  第一类是“传输层”：

  - dispatch overhead：CPU 侧发起 offload/reload 的时间。这个就是你原来 21.9μs 那类数字。
  - per-transfer DMA elapsed time：单个 H2D/D2H copy 的 CUDA event 时间。
  - batch completion time / makespan：一组并发 copy 从开始到全部完成的 wall time。
  - aggregate PCIe bandwidth：总吞吐，判断是否已经打满链路。
  - tail latency：p95/p99 比均值更重要，reviewer 关心的是最坏情况拖慢。
  - time-to-availability：从触发 reload 到 draft model 真正可用于 speculate 的时间。这个比
    单纯 copy 时间更贴近 Nightjar。

  第二类是“请求层”：

  - request latency delta with/without background reload：同一负载下，请求 E2E/TTFT/TPOT
    增加了多少。
  - throughput delta with/without background reload
  - overlap ratio：reload 时间里，有多少被空闲窗口或其他计算隐藏了。
  - burst miss rate：后台 reload 尚未完成时，突发请求到来导致 draft 还不可用的比例。
  - speculation-disabled duration after reload trigger：从触发 reload 到真正恢复
    speculative decoding 的时间。

  如果只能放一个表，我建议表里直接放：

  - Concurrency
  - Per-transfer DMA time mean/p95
  - Concurrent batch completion time mean/p95
  - Aggregate bandwidth
  - Draft re-availability time
  - Request latency increase (%)

  这样 reviewer 会看到“链路代价”和“服务影响”是两回事。