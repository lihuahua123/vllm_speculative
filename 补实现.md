 append-only 的 block-slab growth（分段追加块）

  也就是：

  - 保留原有 KV blocks 完全不动
  - offload draft 后，额外申请一批新的 KV block slabs
  - 只把这些新 blocks 挂到 block allocator / page table / free list 上
  - 后续新 token 如果之前的空间不足了，就只分配到这些新增 blocks
  - 不搬旧 KV，不重建整个大 tensor

  模拟值和真实值： 如果真实实现里还有 slab 分配、zero_()、stream 同步、allocator 抖动，这组数通常会偏乐观