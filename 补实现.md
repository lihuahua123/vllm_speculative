KV Cache 弹性扩缩容方案总结

1. 当前最简单方案：`F.pad` 扩容

- 做法
  - offload draft 后，直接把每层 KV cache 用 `F.pad` 扩大。
  - block allocator / free list 同步增加可用 block。
- 优点
  - 实现最简单。
  - 基本不需要改 attention kernel。
  - 扩容后仍然是单个连续大 tensor，decode 热路径最自然。
- 缺点
  - 扩容时会重建更大的 tensor，并复制旧 KV。
  - 当旧 KV 很大时，扩容时延会明显变差。
  - 容易带来显存抖动、碎片和长尾。
- 结论
  - 最容易落地，但扩容最慢。

2. append-only block-slab growth（分段追加块）

- 做法
  - 保留原有 KV blocks 完全不动。
  - offload draft 后，额外申请一批新的 KV block slabs。
  - 只把这些新 blocks 挂到 block allocator / page table / free list 上。
  - 后续新 token 如果之前的空间不足了，就只分配到这些新增 blocks。
  - 不搬旧 KV，不重建整个大 tensor。
- slab 是什么
  - 一个 slab 就是一整块连续分配的内存。
  - 在这里可以理解为“一批连续的 KV blocks”。
- 优点
  - 扩容时不复制旧 KV。
  - 扩容延迟通常显著好于 `F.pad`。
  - 思路直接，和现有 block allocator 模型比较兼容。
- 缺点
  - 原生 vLLM 的 paged attention 是“单 tensor 内分页”，不是“多 tensor 多 slab 分页”。
  - 如果不改 kernel，就需要做 gather/remap 之类的过渡层，可能拖慢 decode。
  - 缩容不能简单反向做，必须设计 slab-aware 的回收策略。
- 结论
  - 方向是对的。
  - 如果只是过渡版实现，可能会牺牲 decode。

3. append-only slab + gather/remap 过渡实现

- 做法
  - 存储层用 append-only slabs。
  - 每次 attention 前，根据当前 batch 的 `block_tables`，从多个 slabs gather 出临时 dense cache。
  - remap `block_tables` 后继续复用现有 kernel。
- 优点
  - 不需要大改 CUDA kernel。
  - 能较快验证 append-only 思路。
  - 扩容仍然比 `F.pad` 快。
- 缺点
  - 每个 forward 都多一次 gather/remap。
  - decode 热路径可能明显变慢。
  - 更像工程过渡方案，不适合长期正式使用。
- 结论
  - 适合验证，不适合最终上线。

4. append-only slab + slab-aware CUDA kernel

- 做法
  - 保留 append-only slabs。
  - 修改写 KV 的 kernel 和 paged attention kernel，使其支持：
    - `global block id -> slab id -> local block idx`
  - 直接从对应 slab 读写 KV，不再做 gather。
- 优点
  - 扩容快，因为仍然不搬旧 KV。
  - decode 会明显好于 gather/remap 过渡实现。
  - 地址映射本身通常不是大头，主要收益是去掉额外 gather。
- 缺点
  - 改动量比 `F.pad` 大很多。
  - 需要改 CUDA kernel、Python/C++ 封装、block 到 slab 的元数据管理。
  - 调试成本高。
- 结论
  - 如果坚持走 slab 路线，这是更正确的最终形态。

5. eLLM / CUDA VMM 方案

- 做法
  - 不再用传统 `cudaMalloc` 管单一固定物理块。
  - 用 CUDA VMM 在虚拟地址空间上保持连续。
  - 底层按需映射物理 chunks/slabs。
  - 扩缩容本质上变成修改映射关系。
- 优点
  - 对 kernel 来说更接近“真连续虚拟地址”。
  - 理论上比多 slab 手工改 kernel 更优雅。
  - 扩缩容不用 `F.pad` 搬旧 KV。
- 缺点
  - 实现成本很高。
  - 不只是改 KV cache，还要改内存分配层、张量/存储抽象、映射生命周期。
  - 工程改动量通常比直接做 slab-aware kernel 还大。
- 结论
  - 方法更漂亮，但不是小改。
  - 如果只是想先解决当前扩容慢问题，落地成本偏高。

6. 原生 vLLM paged attention 的本质

- 原生 vLLM 一般仍然是单个连续的大 KV cache tensor。
- paged attention 只是逻辑上通过 `block_table` 分页访问 block。
- 它不是天然支持“多 slab / 多 tensor 物理分页”的。
- 所以：
  - 原生是“单 tensor 内分页”
  - 多 slab 方案是“多 tensor 间分页”

7. 是否值得从 `F.pad` 迁移

- 如果更在意扩容时延、长尾、显存抖动：
  - 值得往 append-only 方向走。
- 如果更在意当前 decode 吞吐稳定：
  - 不能直接上 gather/remap 过渡版。
  - 否则可能得不偿失。
- 简单判断
  - 不建议长期停留在 `append-only + gather`。
  - 也不建议轻易退回只用 `F.pad`，除非当前最重要目标就是保住 decode 性能。

8. 缩容建议

- 不建议继续做“任意 block 级全局缩容”。
- 更推荐 slab-aware 的缩容：
  - 只尝试回收尾部 slab。
  - 如果尾部 slab 完全空闲，直接释放。
  - 如果尾部 slab 还有活跃 block，只迁移这些活跃 block 到前面 free blocks。
  - 然后释放整个尾部 slab。
- 优点
  - 更符合 append-only 设计。
  - 避免又回到“大量搬旧 KV”的老路。

9. 现实可行的折中建议

- 如果当前主要问题是 `F.pad` 扩容慢，但又担心 decode 回退：
  - 先保留 `F.pad` 路线。
  - 优先优化扩缩容策略，而不是立刻大改实现。
- 可做的策略优化
  - 降低扩容频率。
  - 每次扩容多扩一点。
  - 增加 hysteresis，避免频繁扩缩。
  - 缩容少做，或者只在低负载时做。
  - 预留 spare slabs / virtual blocks，需要时直接启用。

10. 当前整体判断

- `F.pad`
  - 最简单，但扩容慢。
- append-only + gather
  - 扩容快，但可能拖 decode。
- append-only + slab-aware kernel
  - 是更像样的工程终态，但实现成本高。
- CUDA VMM
  - 理念最好，但改动最大。

现阶段更现实的建议：

- 短期：先优化扩缩容策略，减轻 `F.pad` 触发频率。
- 中期：如果确实要做 append-only，就直接朝 slab-aware kernel 版本演进，不建议长期使用 gather 过渡版。
- 长期：如果团队能接受较大改造，再考虑 CUDA VMM 方案。

补充

- 模拟值和真实值：
  如果真实实现里还有 slab 分配、zero_()、stream 同步、allocator 抖动，这些方案的理论收益通常都会比理想估计更保守。
